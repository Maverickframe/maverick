.DS_Store
node_modules
npm-debug.log
*.sass-cache*
packaged
.vscode
.env
build
old/
# HubSpot CRM token — kept out of git (push protection); set via wp-config on server
forms/hubspot-credentials.php

# Retired amoCRM credentials (2026-07-15) — never re-add.
forms/amo-credentials.php

# SMTP App Password + lead recipients — out of git, lives on the server
forms/notify-credentials.php
